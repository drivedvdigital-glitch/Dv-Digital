# RELATÓRIO 11 — Be Wiser Clips · Produtos relacionados (fecha o Product list) — 1:30

ID `dsTro20gcDI` · PageFly 29/Shopify 37 (Winter '26).

## A lição forte (rel. 09+10+11 juntos)

**Um elemento, três casos de uso.** Product source tem EXATAMENTE três opções — All
(padrão), Custom collection, Related products — e combinado com Layout type
(Grid|Slideshow) cobre lista, carrossel e relacionados. Quem configurou um, configurou
os três. **Adotado para o nosso bloco de produtos.**

## Inspetor completo (o que faltava)

- Alignment 9 pontos, centro "+" · Item spacing slider+num+un, padrão 30px
- **Exclude current product: checkbox DESABILITADA com o motivo ao lado** ("This
  setting only works on a product page")
- Loading mode: select, padrão "None", aviso "só na página publicada" (opções não
  abertas — pendência)
- Aviso dos 50 produtos DUAS vezes no painel, redações diferentes (exibição ×
  carregamento) — redundância proposital: dois controles no mesmo teto
- Estado inicial no canvas = **card de produto de exemplo funcional** (miniatura, nome,
  preço, Add to Cart), não caixa vazia — o usuário vê a forma antes de ligar os dados
- Custom product visível mesmo em Related products (produto de referência) — nome do
  campo não ajuda; nomear melhor no nosso
- Anúncios de apps de novo, ordem invertida = rotação de inventário (não copiar)

## REGRA ADOTADA no D&VFly (3ª ocorrência independente → é regra deles, vira nossa)

**Desabilitar explicando, nunca esconder.** (AEO "only works on product pages",
View live cinza, Exclude current product.) Registrada no CLAUDE.md.

## Valores padrão confirmados

source All · per loading 4 · Grid · per row 4 · Fill last row No · spacing 30px ·
Loading None · Exclude desmarcado/desabilitado.

## Pendências abertas

Opções do Loading mode (nova) · controles do Slideshow (rel. 09) · painel do formulário
deles (rel. 08) · Customer × Contact form (rel. 08) · ~2 elementos Shopify cortados.
