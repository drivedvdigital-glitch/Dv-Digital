/**
 * Names shared between the server (which writes them into themes) and the
 * editor UI (which shows them). No imports, so a browser bundle can carry this
 * file without dragging the API client along.
 */

/** The templateSuffix a chrome-less page gets. */
export const SOLO_SUFFIX = 'dvfly-solo';
